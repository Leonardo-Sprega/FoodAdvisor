

<nav class="navbar navbar-expand-lg fixed-top navbar-light bg-white" id="navbrist">

    <div class="container-fluid">
        <% unless current_page?(new_user_registration_path) || current_page?(new_user_session_path)%>
            <%= link_to "FoodAdvisor", home_index_path, class:"navbar-brand mx-auto" %>

        <% else %>
        <div class="navbar-logo">
            <%= link_to "FoodAdvisor", home_index_path, class:"navbar-brand mx-auto", style:"font-size:30px !important" %>
        </div>
        <% end %>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ms-auto">
                        <% if user_signed_in? %>

                                <% if current_user.ristoratore==="true" %>
                                    <%= link_to "Hai un ristorante?", new_ristorante_url , class:"nav-link nav-hv", id:"new-rist"%>
                                <% else %>
                                    <%= link_to "Hai un ristorante?", edit_user_registration_path , class:"nav-link nav-hv", id:"new-rist"%>
                                <% end %>

                            <i class="fa-solid fa-user fa-xl" style="color: orange;"></i>
                            <%= button_to "#{
                                if current_user.nome != '' 
                                    current_user.nome 
                                else 
                                    current_user.email 
                                end 
                            }",
                            profilo_index_path,class:"icona_username",method: :get %>

                            <%= button_to "Log out", destroy_user_session_path,class:"nav-btn", :method => :delete %>
                        <% end %>

                  
                        <% if !user_signed_in? &&  !(current_page?(new_user_registration_path) || current_page?(new_user_session_path)) %>
                            <%= button_to "Login", new_user_session_path, class:"nav-btn",method: :get %>
                        <% end %>
                
            </ul>
        </div>
    </div>

</nav>



