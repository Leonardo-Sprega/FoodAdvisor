<%= form_with(model: foto_recensione) do |form| %>
  <% if foto_recensione.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(foto_recensione.errors.count, "error") %> prohibited this foto_recensione from being saved:</h2>

      <ul>
        <% foto_recensione.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :url, style: "display: block" %>
    <%= form.text_field :url %>
  </div>

  <div>
    <%= form.label :recensione_id, style: "display: block" %>
    <%= form.text_field :recensione_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
