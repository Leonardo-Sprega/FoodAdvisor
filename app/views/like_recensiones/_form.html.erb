<%= form_with(model: like_recensione) do |form| %>
  <% if like_recensione.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(like_recensione.errors.count, "error") %> prohibited this like_recensione from being saved:</h2>

      <ul>
        <% like_recensione.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :recensione_id, style: "display: block" %>
    <%= form.text_field :recensione_id %>
  </div>

  <div>
    <%= form.label :utente_id, style: "display: block" %>
    <%= form.text_field :utente_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
