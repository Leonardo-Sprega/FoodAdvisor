<%= form_with(model: tipo_cucina) do |form| %>
  <% if tipo_cucina.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(tipo_cucina.errors.count, "error") %> prohibited this tipo_cucina from being saved:</h2>

      <ul>
        <% tipo_cucina.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nome, style: "display: block" %>
    <%= form.text_field :nome %>
  </div>

  <div>
    <%= form.label :descrizione, style: "display: block" %>
    <%= form.text_area :descrizione %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
