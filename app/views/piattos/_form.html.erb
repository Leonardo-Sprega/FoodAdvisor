<%= form_with(model: piatto) do |form| %>
  <% if piatto.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(piatto.errors.count, "error") %> prohibited this piatto from being saved:</h2>

      <ul>
        <% piatto.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nome, style: "display: block" %>
    <%= form.text_field :nome %>
  </div>

  <div>
    <%= form.label :descrizione, style: "display: block" %>
    <%= form.text_area :descrizione %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
